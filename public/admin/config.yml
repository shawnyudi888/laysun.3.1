# 这个文件现在可以非常简单，因为配置都在 index.html 里了
# 甚至可以留空，但保留一个简单版本备用
backend:
  name: github
  repo: shawnyudi888/laysun.3.1
  branch: main
  auth_endpoint: /api/auth
  base_url: https://www.laysun.co

media_folder: "public/uploads"
public_folder: "/uploads"

# 集合配置可以省略，因为已经在 index.html 中定义了
